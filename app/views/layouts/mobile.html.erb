<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <%= yield :head %>
    <title>
      <%= content_for?(:title) ? yield(:title) : "Fastladder Mobile" %>
    </title>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
